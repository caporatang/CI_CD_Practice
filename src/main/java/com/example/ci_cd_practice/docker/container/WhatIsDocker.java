package com.example.ci_cd_practice.docker.container;

/**
 * packageName : com.example.ci_cd_practice.docker.container
 * fileName : WhatIsDocker
 * author : taeil
 * date : 12/31/23
 * description :
 * =======================================================
 * DATE          AUTHOR                      NOTE
 * -------------------------------------------------------
 * 12/31/23        taeil                   최초생성
 */
public class WhatIsDocker {
    // Docker는 무엇인가?

    // 여러 계층의 Application을 container로 분리, 연결하여 실행하는 MSA 프로젝트에 유용하다.
    // Application의 Infra(runtime)는 Image를 통해 제공하고 Public or Private 하게 공유 가능하다.
    // Github과 유사한 방식(open share)으로 Docker Hub에서 제공한다.
    // 이렇게 제공된 Image를 기반으로 Application 서비스를 제공, 이를 컨테이너화(Containerization) 할 수 있다
}